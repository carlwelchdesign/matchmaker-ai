# Argent mobile

Flutter application for iOS and Android.

From this directory:

```bash
flutter analyze
flutter test
flutter run
```

Product workflows, generated API clients, and final Nocturne theme adapters are added only through their owning tickets.
