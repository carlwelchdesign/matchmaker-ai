# Contracts

The OpenAPI source and generated TypeScript/Dart clients will be added under `ARG-102`.

This package is intentionally empty in the platform-foundation ticket so transport contracts do not leak into the framework-light domain package.
