# Shared configuration

Cross-workspace TypeScript defaults currently live in the root `tsconfig.base.json`. Additional lint, test, and build configuration belongs here only when at least two consumers require the same rule set.
