# Design system

The canonical Nocturne tokens and generated web/Dart adapters are owned by `ARG-118`.

This boundary exists now so product code does not invent local token names before the generator and drift checks are approved.
